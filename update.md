# 更新记录

## v1.7 (当前版本)
- 新增Flite语音引擎自动下载功能，当检测不到Flite引擎时可自动从网络下载
- 优化了Flite引擎的路径查找逻辑，按"项目根目录→runtime目录→系统路径"优先级查找
- 改进了语音引擎调用机制，使用QProcess替代system函数避免CMD窗口闪烁
- 增强了语音引擎的稳定性，在Flite引擎失效时自动回退到SAPI引擎
- 添加了详细的错误提示和用户引导，提升用户体验

## v1.6 
- 修复了Flite语音引擎调用时出现CMD窗口闪烁但无声音的问题
- 优化了Flite引擎的路径查找逻辑，优先使用项目根目录下的flite.exe
- 增强了语音引擎的稳定性，在Flite引擎失效时自动回退到SAPI引擎
- 改进了外部程序调用方式，使用QProcess替代system函数避免界面闪烁
- 添加了语音引擎执行错误的提示和处理机制
- 新增Flite自动下载功能，当检测不到flite主程序时可自动从网络下载

## v1.5 
- 在编辑词库时，自动在appdata/temp下生成临时词库文件
- 文本输入框的任何更改都会实时保存到临时词库中
- 退出时询问是否保存临时词库到指定位置
- 添加"词库另存为"功能，可将当前词库保存到任意位置
- 新增Flite朗读引擎选项，可在SAPI和Flite之间切换
- 更新了CMakeLists.txt构建脚本，支持Flite程序部署
- 添加了flite_readme.txt说明文档，指导用户部署Flite引擎
- 更新了关于界面和指南界面的相关说明

## v1.4 
- 界面字体统一为微软雅黑，提升视觉效果
- 主界面按钮尺寸缩小2px，优化布局
- 为所有界面元素应用了统一的字体样式

## v1.3 
- 添加了指南界面，方便用户快速了解软件功能和使用方法
- 在主界面添加了"使用指南"按钮，提供详细的操作说明
- 指南界面包含程序简介、主要功能、使用步骤、设置选项、词库文件说明和注意事项

## v1.2 
- 添加了构建后自动部署 Qt 库和 C++ 运行时库的功能，确保程序能在没有运行库的电脑上正常运行
- 添加了构建后自动数字签名功能，提升软件安全性和可信度
- 优化了词库文件加载机制，支持递归遍历整个词库文件夹及其子文件夹
- 改进了词库文件选择交互，用户可在界面中直接选择可用的词库文件，无需打开系统文件对话框
- 修复了 PowerShell 脚本签名命令中的语法错误
- 完善了构建流程，确保所有依赖项正确处理

## v1.1
- 实现了完整的词库目录检测、配置文件管理和退出时保存词库功能
- 添加了配置文件功能，使用 config.ini 保存和加载设置（包括主题设置、朗读时间间隔等）
- 实现了词库文件有效性检查逻辑
- 添加了缓存机制保存用户添加的单词
- 在程序退出时询问用户是否保存词库
- 修复了 VBS 脚本编译错误问题，通过 Base64 编码传递文本避免字符转义问题
- 增强了测试界面功能，添加了上一个词、下一个词、暂停/继续、设置等按钮
- 实现了答案显示界面和返回功能
- 为主页添加了动态欢迎语动画效果
- 为按钮添加了边界和样式，并实现了窗口缩放适配
- 恢复了朗读时间间隔设置功能，并移除了语速设置选项

## v1.0 (初始版本)
- 创建了基本的英语听写练习应用程序
- 实现了基本的用户界面，包括主界面、单词管理界面、测试界面和答案界面
- 实现了基本的听写测试功能，包括单词朗读、倒计时、测试控制等
- 实现了单词管理功能，包括添加、删除、清空、导入和导出单词
- 实现了主题切换功能，支持浅色和深色主题
- 实现了基本的设置功能，包括朗读时间间隔设置
- 使用 Windows SAPI 和 VBScript 实现了文本转语音功能